# 배포 가이드 (Vercel)

## 1. 저장소 준비

```bash
cd C:\projects\tbm
git init
git add .
git commit -m "TBM 안전점검 기록 시스템 초기 구현"
```

GitHub에 비공개 저장소를 만들고 푸시합니다.

> `.env.local`과 `public/uploads`는 `.gitignore`에 들어 있어 올라가지 않습니다.

## 2. Vercel 프로젝트 생성

1. [vercel.com](https://vercel.com)에서 저장소를 import 합니다.
2. Framework는 Next.js로 자동 인식됩니다. 빌드 설정은 건드릴 필요 없습니다.

## 3. 데이터베이스

Neon(권장) 또는 Vercel Postgres를 씁니다. 리전은 서울을 고르세요.

Vercel 프로젝트 → Settings → Environment Variables에 추가:

| 이름 | 값 |
| --- | --- |
| `DATABASE_URL` | Neon의 **Pooled** connection string (`?sslmode=require` 포함) |
| `AUTH_SECRET` | `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"` 결과 |
| `NEXT_PUBLIC_APP_URL` | 실제 도메인 (예: `https://tbm.회사도메인.com`) |

`NEXT_PUBLIC_APP_URL`은 **QR에 인코딩되는 주소**입니다. QR을 인쇄한 뒤에 도메인을
바꾸면 인쇄물을 다시 뽑아야 하니, 배포 전에 최종 도메인을 정하세요.

## 3-1. 주소 → 좌표 변환 (권장)

관리 화면에서 주소를 넣고 **좌표 찾기**를 누르면 위도·경도가 자동으로 채워집니다.

키를 넣지 않으면 OpenStreetMap을 쓰는데, **국내 도로명 주소는 대부분 못 찾습니다.**
카카오 키를 넣는 것을 권합니다. 무료이고 5분이면 됩니다.

1. [developers.kakao.com](https://developers.kakao.com) 로그인 → **내 애플리케이션** →
   애플리케이션 추가
2. 만든 앱 → **앱 키** → **REST API 키** 복사
3. 환경변수에 추가

```
KAKAO_REST_API_KEY="복사한_REST_API_키"
```

카카오를 쓰면 도로명·지번 주소는 물론 공장 이름으로도 찾을 수 있습니다.

> 좌표는 **관리 → 사업장 설정**에서 언제든 직접 고칠 수 있습니다. 지도 앱에서 현장을
> 길게 눌러 나오는 좌표를 넣어도 됩니다.

## 4. 사진 저장소 (Vercel Blob)

Vercel 프로젝트 → Storage → **Blob** 생성.
연결하면 `BLOB_READ_WRITE_TOKEN`이 자동으로 주입됩니다.

> 이 값이 없으면 사진이 서버 파일시스템(`public/uploads`)에 저장됩니다.
> Vercel은 파일시스템이 요청마다 초기화되므로 **운영에서는 반드시 Blob을 붙여야
> 합니다.** 로컬 개발 편의를 위한 fallback입니다.

## 5. 스키마 반영

로컬에서 운영 DB를 향해 한 번 실행합니다.

```bash
# .env.local의 DATABASE_URL을 운영 값으로 잠시 바꾼 뒤
npm run db:push
```

이후 스키마를 바꿀 때는 마이그레이션 파일로 관리하는 편이 안전합니다.

```bash
npm run db:migrate -- --name 변경내용
```

## 6. 초기 데이터

**모두 화면에서 넣을 수 있습니다.** 시드 스크립트를 고칠 필요 없습니다.

본사 계정으로 로그인한 뒤 **관리** 탭에서 순서대로 진행하세요. 화면 위에도 같은
순서가 안내되어 있습니다.

| 순서 | 화면 | 하는 일 |
| --- | --- | --- |
| 1 | 관리 → 사업장 | 공장을 만들고, 설정에서 **위도·경도**를 넣습니다 |
| 2 | 관리 → 계정 | 안전관리자·팀장 로그인 계정을 만듭니다 |
| 3 | 관리 → 작업팀 | 팀을 만들고 팀장을 지정합니다 |
| 4 | 관리 → 작업자 명부 | 엑셀 양식을 내려받아 채운 뒤 한 번에 올립니다 |
| 5 | 관리 → 교육 템플릿 | 매일 교육할 항목을 현장에 맞게 고칩니다 |
| 6 | 관리 → 출석 QR | QR을 인쇄해 지문인식기 옆에 붙입니다 |

### 데모 데이터 지우기

시드로 만든 제1공장·제2공장은 **관리 → 사업장**에서 삭제합니다. 확인을 위해
사업장 이름을 정확히 입력해야 지워집니다. 소속 팀·작업자·TBM 기록·계정·QR이
모두 함께 사라지니, 실제 데이터를 넣기 전에 정리하세요.

본사 계정(`hq@example.com`)은 사업장 소속이 아니라 삭제되지 않습니다. 실제 본사
계정을 만든 뒤 **관리 → 계정**에서 데모 계정을 잠그세요.

### 작업자 엑셀 업로드

**관리 → 작업자 명부 → 엑셀로 일괄 등록**

1. **양식 내려받기** — 현재 등록된 팀 목록과 작성 안내가 함께 들어 있습니다
2. 채워서 **올리기** — 결과를 먼저 보여 주고, 확인 후에 반영합니다
3. 미리보기에서 줄별로 **신규 / 수정 / 오류**를 확인하고 **반영하기**

알아 둘 점:

- 필수 항목은 **이름**뿐입니다. 열 이름은 `성명`·`사원번호`·`연락처`처럼 흔한
  표기도 알아서 인식합니다.
- **사번**을 넣어 두면 다시 올릴 때 같은 사람으로 인식해 수정 처리합니다.
  사번이 없으면 이름으로 찾는데, 동명이인이 있으면 오류로 표시됩니다.
- **휴대폰**은 뒤 4자리를 QR 출석 시 본인 확인에 씁니다. `01012345678`처럼 붙여
  써도 자동으로 정리됩니다.
- **팀이 비어 있으면 QR을 찍어도 출석이 기록되지 않습니다.** 반드시 채우세요.
- `없는 팀은 자동으로 만들기`를 켜면 파일에 있는 새 팀 이름이 그대로 만들어집니다.
  처음 설정할 때 편합니다. 다만 **팀장은 따로 지정해야 합니다.**
- `파일에 없는 인원은 비활성화하기`는 전체 명부를 통째로 갱신할 때만 켜세요.
  과거 출석 기록은 남습니다.
- 오류가 있는 줄은 건너뛰고 나머지만 반영됩니다. 한 번에 최대 2,000명.
- CSV도 됩니다. 엑셀에서 저장한 UTF-8 CSV를 그대로 올리면 됩니다.

## 7. 배포 후 체크리스트

- [ ] 로그인이 되는가
- [ ] 관리 → 사업장 설정에서 **위도·경도**를 넣었는가 (안 넣으면 위치 검증이 꺼짐)
- [ ] 관리 → 출석 QR에서 QR을 인쇄해 지문인식기 옆에 부착했는가
- [ ] 인쇄한 QR을 실제 휴대폰으로 스캔해 출석이 기록되는가
- [ ] 팀장 계정으로 사진을 올렸을 때 촬영 시각·거리가 표시되는가
- [ ] 안전관리자 계정 결재함에 상신 건이 보이는가
- [ ] 관리 → 교육 템플릿의 내용이 현장 실정에 맞는가

## 운영 중 자주 하는 일

**QR을 새로 뽑아야 할 때** — 관리 → 출석 QR에서 지점을 추가하면 새 토큰이
발급됩니다. 기존 QR이 유출됐다면 해당 지점을 "사용 중지"하고 새로 만드세요.

**한 지문인식기를 여러 법인이 같이 쓸 때** — 관리 → 출석 QR에서 해당 QR의
**담당 사업장 바꾸기**를 눌러 같이 쓰는 법인을 모두 고르세요. 주소가 같은
사업장은 한 번에 선택할 수 있습니다. 본사 관리자만 바꿀 수 있습니다.

- QR 하나로 여러 법인의 명부가 함께 뜨고, 작업자는 회사명을 보고 본인을 고릅니다
- 출석은 **본인이 속한 법인의 TBM**에 기록됩니다. QR 소유 법인이 아닙니다
- 마감·지각 기준도 **각자 자기 사업장 설정**을 따릅니다

**작업자가 퇴사했을 때** — 관리 → 작업자 명부에서 "비활성화". 과거 출석 기록은
그대로 남습니다.

**교육 내용을 바꿔야 할 때** — 관리 → 교육 템플릿에서 수정. **이미 생성된 오늘
TBM에는 반영되지 않고 내일부터 적용됩니다.** 과거 기록의 무결성을 위해 템플릿은
생성 시점에 스냅샷으로 복사되기 때문입니다.

## 알아 둘 제약

- **QR 체크인은 본인 대신 찍어 주는 것을 막지 못합니다.** 기기 쿠키로 사람을
  기억하기 때문에, 한 사람이 여러 기기에서 다른 사람 이름으로 찍을 수 있습니다.
  지문인식기와 병행하는 보조 수단으로 보고, 진짜 증빙은 현장 사진의 EXIF로
  받는 구조입니다.
- **사진 EXIF는 위조할 수 있습니다.** 전용 앱을 쓰면 촬영 시각·GPS를 조작할 수
  있습니다. 다만 카톡 재전송·캡처처럼 흔한 우회는 EXIF 소실로 잡힙니다.
- **사진 원본이 그대로 올라가야 검증이 됩니다.** 카카오톡·일부 갤러리 앱으로
  공유한 사진은 EXIF가 제거됩니다. 카메라로 바로 찍어 올리도록 안내하세요.

## 8. 월간 백업

무료 요금제에는 자동 백업이 없습니다. Neon 무료는 되돌릴 수 있는 기간이 짧고
Blob은 지우면 그걸로 끝이라, **한 달에 한 번** 통째로 내려받아 두세요.

```
npm run backup                  전체 (DB + 사진)
npm run backup -- --no-photos   DB만, 빠릅니다
npm run backup -- --out D:/백업  저장 위치 지정
```

만들어지는 것:

```
backup/2026-08-31/data.json     모든 표의 내용 (사람이 읽을 수 있는 JSON)
backup/2026-08-31/photos/…      현장 사진 원본
backup/2026-08-31/README.txt    무엇이 몇 건 들어 있는지
```

- **로그인 비밀번호(해시)는 백업에 넣지 않습니다.** 유출되면 그대로 공격에
  쓰이기 때문입니다. 계정을 되살릴 때는 비밀번호를 새로 정하면 됩니다.
- 백업 폴더는 `.gitignore`에 있습니다. 작업자 이름·연락처가 들어 있으니
  저장소나 공유 폴더에 그대로 올리지 마세요.
- 결재가 끝난 문서는 **결재완료함에서 합쳐 내려받은 PDF**가 사실상의 원본
  보관본입니다. 월말에 그것도 함께 받아 두면 이중으로 안전합니다.

### 한 달치 내려받기

서버리스 응답 크기 한도(약 4.5MB) 때문에 **나눠서** 받습니다. 한 묶음에
몇 건이 들어가는지는 사진 장수에 따라 다릅니다.

| 문서            | 건당 크기 | 한 묶음 |
| --------------- | --------- | ------- |
| 순찰일지        | 약 115KB  | 20건    |
| TBM (사진 없음) | 약 136KB  | 20건    |
| TBM (사진 2장)  | 약 322KB  | 11건    |
| TBM (사진 4장)  | 약 508KB  | 7건     |

`앞에서 N건 선택` → `내려받기` → `다음 N건 (12~22 / 264)` → `내려받기` …
버튼이 들어갈 만큼만 집고 어디까지 갔는지 세어 주므로, 순서대로 누르기만
하면 빠뜨리는 문서 없이 한 달치가 끝납니다.

작업팀 10개 기준 한 달이면 TBM 약 220건, 순찰일지 약 44건입니다.
**주 단위로 나눠 받으면** 한 번에 5회 안쪽으로 끝나 훨씬 수월합니다.

> 합쳐 받는 PDF는 사진을 800px(인쇄 약 230dpi)로 줄여 담습니다. 지면에서
> 가로 8.8cm로 찍히므로 인쇄에는 충분하고, 그만큼 한 번에 더 많이 담깁니다.
> 한 건씩 받을 때는 1041px(300dpi) 그대로입니다.

## 9. 저장 용량

**`hq` 계정으로 로그인하면 대시보드 맨 아래에 지금 쓰는 용량이 나옵니다.**
어림잡은 값이 아니라 저장소에 직접 물어본 실제 크기입니다. 되찾을 수 있는
용량이 있으면 실행할 명령까지 같이 띄웁니다.

병목은 DB가 아니라 사진입니다.

| | 하루 증가 | 무료 한도 기준 |
| --- | --- | --- |
| DB (Neon 0.5GB) | 약 100KB | 15년치 |
| 사진 (Blob 1GB) | 장당 약 180KB | 아래 참고 |

사진은 올릴 때 **긴 변 1280px·품질 80**으로 줄여 저장합니다(EXIF는 보존).
폰 원본 3~5MB가 200KB 아래로 떨어져, 하루 20장씩 올려도 1년 가까이 씁니다.
줄이는 값은 `app/api/tbm/[id]/photos/route.ts`의 `STORE_MAX_PX`에 있습니다.

줄이는 일은 **브라우저에서 먼저** 합니다. Vercel이 4.5MB 넘는 요청은 함수에
닿기도 전에 자르기 때문입니다(폰 원본 한 장이 그 언저리라 사유 없이 실패하곤
했습니다). canvas로 다시 그리면 EXIF가 날아가므로 원본 앞 128KB를 함께 보내
촬영 시각·좌표를 읽고, 저장할 파일에도 그 EXIF를 도로 끼웁니다.

### 한 일지에 붙는 사진

| | 상한 | 비고 |
| --- | --- | --- |
| 직접 올린 사진 | 2장 | 받은 사진과 무관하게 늘 확보됩니다 |
| 받은 사진 | 2장 | 같은 주소 법인이 합동 TBM에서 공유한 것 |

같은 주소에 법인이 넷인 공장(진천)에서는 옆 법인이 보낸 사본이 자리를 다
먹어 정작 자기 사진을 못 올리는 일이 있었습니다. 그래서 따로 셉니다.
값은 `lib/tbm.ts`의 `MAX_OWN_PHOTOS`·`MAX_SHARED_PHOTOS`에 있습니다.
늘리면 결재 PDF가 두꺼워져 한 묶음에 담기는 건수가 줄어듭니다.

> Vercel Hobby는 약관상 비상업용입니다. 회사 업무로 계속 쓰신다면 Pro 전환을
> 검토하세요. 저장 용량도 함께 늘어납니다.

### 주인 없는 파일

사진을 화면에서 지우면 기록은 사라지지만 **파일은 저장소에 남습니다**.
2026-08-26 이후로는 지울 때 파일도 함께 지우지만(합동 TBM 사본이 같은 파일을
쓰고 있으면 마지막 하나가 지워질 때까지 남겨 둡니다), 그 전에 쌓인 것은
아래 명령으로 치웁니다.

```bash
npm run prune-orphans              # 미리보기
npm run prune-orphans -- --confirm # 실제 정리
```

어느 기록도 가리키지 않는 파일만 지웁니다. 화면에서 사라지는 것은 없고
되찾는 것은 용량뿐이라, 백업 확인 없이 바로 실행해도 됩니다.

## 10. 사진 정리 (2개월 보관)

사진만 두 달치를 남기고 그보다 오래된 것은 백업한 뒤 파일을 지웁니다.
**TBM 본문·출석·점검 결과는 지우지 않습니다** — 텍스트는 15년을 둬도 안 찹니다.

지운 사진 자리에는 `보관 처리됨`이 남고, **촬영 시각·위치·검증 경고는 그대로**
보존되어 사후 확인이 됩니다.

자동으로 돌지 않습니다. 관리 화면에 정리 시점이 뜨면 그때 사람이 실행합니다.

```
npm run backup                                          ① 반드시 먼저
npm run prune-photos -- --before 2026-10-01             ② 미리보기
npm run prune-photos -- --before 2026-10-01 --confirm   ③ 실제 정리
```

- 지울 사진이 백업 폴더에 **파일 단위로 있는지 확인**합니다. 하나라도 없으면
  아무것도 지우지 않고 멈춥니다.
- `--confirm` 없이는 절대 지우지 않습니다.
- **한 번 지운 사진은 되돌릴 수 없습니다.**

> 보존 의무는 "기록을 보관하는 것"이지 "이 시스템에 두는 것"이 아닙니다.
> 다만 TBM이 안전보건교육 실시 기록으로 분류되면 법정 보존 기간이 있으니,
> 백업본을 그 기간만큼 안전하게 보관하세요.
